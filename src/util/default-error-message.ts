export const defaultErrorMessage = (action: string, entity: string) => {
  return `Ocorreu um erro ao ${action} ${entity}. Tente novamente mais tarde. Se o problema persistir entre em contato com suporte.`;
};
